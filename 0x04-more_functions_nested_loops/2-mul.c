#include "main.h"

/**
 * mul - multiplies two integers
 * @k: The first number to multiply
 * @h: The second number to multiply with first number
 * Return: The result of multiplication between k param and h param
 */

int mul(int h, int k)

{
	return (k * h);
}
